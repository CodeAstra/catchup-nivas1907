<%= turbo_stream.replace"friendbutton_#{@user.id}" do%>
    <%= turbo_frame_tag "fbutton_#{@user.id}" do %>
         <%= button_to "Add Friend",friends_path(fid:@user.id),method: :post %>
    <%end%>
<%end%>