<% if @post.persisted? %>
  <%= turbo_stream.prepend "post", partial: "posts/post", locals: { post: @post, highlight: true } %>
  <%= turbo_stream.remove Post.new %>
<% else %>
  <%= turbo_stream.replace Post.new, partial: "posts/form", locals: { post: @post } %>
<% end %>
